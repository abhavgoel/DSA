#include<bits/stdc++.h>
using namespace std;

//shifts the bits to left by given position and trailing zeroes added which is equal to the shift

int main()
{
    int x=3;
    cout<<(x<<1)<<endl;
    cout<<(x<<2)<<endl;
    return 0;
    //the result (x<<y) is equivalent to x*2^y....where y is the number of time shifted

}