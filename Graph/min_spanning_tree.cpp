//a tree which has n-1 edges and n nodes is called a Spanning tree, and every node is reacheable from other

// a graph can have more than 1 spanning tree


//the min spanning tree - MST is the tree which has the smallest edge weight sum

